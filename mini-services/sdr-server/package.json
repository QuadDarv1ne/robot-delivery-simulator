{
  "name": "sdr-server",
  "version": "1.0.0",
  "description": "RTL-SDR Geoanalytics server for robot delivery simulator",
  "main": "index.ts",
  "scripts": {
    "start": "bun run index.ts",
    "dev": "bun --watch index.ts"
  },
  "dependencies": {
    "socket.io": "^4.8.3"
  },
  "devDependencies": {
    "bun-types": "^1.3.4"
  }
}
