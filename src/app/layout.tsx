import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";
import { Providers } from "@/components/providers";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Robot Delivery Simulator - Симулятор робота-доставщика",
  description: "3D симулятор движения робота-доставщика с интеграцией внешних систем управления. Unity WebGL + ROS/ROS2 + API.",
  keywords: ["Robot Simulator", "Unity WebGL", "ROS", "ROS2", "Delivery Robot", "Autonomous Robot"],
  authors: [{ name: "Robot Simulator Team" }],
  icons: {
    icon: "https://z-cdn.chatglm.cn/z-ai/static/logo.svg",
  },
  openGraph: {
    title: "Robot Delivery Simulator",
    description: "3D симулятор робота-доставщика с внешним управлением",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="ru" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        <Providers>
          {children}
        </Providers>
        <Toaster />
      </body>
    </html>
  );
}
